import pygame 
import os 

def attack_enemy(): 
    if Z is pressed:
    if "z" is pygame.PRESSDOWN() 
        attack frames and hitbox
        if in enemy hitbox: hit enemy
        else: nothing
        if Z is held: 
            attack 2 frames and hitbox
            if in enemy hitbox: hit enemy
            else: nothing
            if Z is still held: 
                attack 3 frames and hitbox
                if in enemy hitbox: hit enemy
                else: nothing  