import pygame, os, random

enemy dictionary

define attack_player: 
    enemy 

define action:
    act = random(1-3)
    
    if act = 1: 
        attack_player
    else if act = 2: 
        block
    else if act = 3
        walk randomly
    
    clear act variable

define close_in: 
    if player is 20px from player, do action
    else if player is 320px from enemy, move to player